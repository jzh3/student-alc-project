# Pin these to the versions you actually used.
# Run `pip freeze | grep -Ei "pandas|matplotlib|numpy|jupyter"` and paste the
# real version numbers in place of the ones below.

pandas==2.2.3
matplotlib==3.9.2
numpy==2.1.3
jupyter==1.1.1
